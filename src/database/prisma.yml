#endpoint: ${env:PRISMA_ENDPOINT}
#endpoint: https://us1.prisma.sh/lucas-mafra-3552e0/cubo-api/stage
endpoint: https://eu1.prisma.sh/lucas-mafra-3552e0/cubo-api/stage
datamodel: datamodel.graphql
secret: ${env:PRISMA_SECRET}

hooks:
  post-deploy:
    - graphql get-schema
    - graphql codegen