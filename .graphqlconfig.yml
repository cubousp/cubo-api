projects:
  app:
    schemaPath: src/schema.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
  database:
    schemaPath: src/infrastructure/database/generated/prisma.graphql
    extensions:
      prisma: prisma.yml
      codegen:
        output:
          binding: src/infrastructure/database/generated/prisma.ts
        generator: prisma-binding
        language: typescript